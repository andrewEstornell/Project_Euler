import numpy as np
import sympy as sp


# (i)  Each of the three terms is prime
# (ii) each number is a permutation of the others
# Find such a sequence that is not 1487, 4817, 8147

#def gen_sequence():

# Checks if a an be are permutations of one another
#def is_permutation(a, b):
#    b_dig = np.digitize(b)

